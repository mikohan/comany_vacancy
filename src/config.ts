export const durationPage: number = 0.5; // Animation between pages

export const drawerWidth: number = 240; // Width of drawer

export const isDrawerOpen = false;

export const defaultDarkTheme = true; // Dark them by default
